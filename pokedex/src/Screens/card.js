import React from "react"
import { useTheme } from '@material-ui/core/styles';

function Card() {
  const theme = useTheme();

  return (
     <div>
         funciona details
     </div>
  )
}

export default Card;
